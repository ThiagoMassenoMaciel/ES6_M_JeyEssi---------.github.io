export const division = (num, nunn) => num/nunn;

export const sum = (num, nunn) => num+nunn;

export const Sub = (num, nunn) => num-nunn;

export const mult = (num, nunn) => num*nunn;

export const exponential = (num, nunn) => num**nunn;



/*
OR 

export { each name variable , separated with a comma }
*/ 